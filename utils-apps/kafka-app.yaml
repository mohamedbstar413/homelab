apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kafka-app
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/mohamedabdelsatar/homelab.git
    targetRevision: main
    path: kafka/strimzi-kafka-operator

    helm:
      valueFiles:
        - values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: 'kafka'

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true