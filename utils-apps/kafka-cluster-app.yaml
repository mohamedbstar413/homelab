apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kafka-cluster-app
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/mohamedbstar413/homelab.git
    targetRevision: main
    path: kafka/kafka-resources

  destination:
    server: https://kubernetes.default.svc
    namespace: 'kafka'

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true