apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  selector:
    app: api-gateway
    tier: backend
  ports:
    - protocol: TCP
      port: 4004
      targetPort: 4004
      name: http